export type LevelystDeployMode = "local" | "public" | "demo"

export const LEVELYST_DEMO_READONLY_MESSAGE = "Levelyst demo mode is read-only on the public deployment."
export const LEVELYST_DEMO_READONLY_HINT =
  "Clone the repository and run locally to create projects, save edits, and enable full AI-backed generation."

export const LEVELYST_PUBLIC_SESSION_MESSAGE = "Projects are saved in this browser on the public app."
export const LEVELYST_PUBLIC_KIOSK_MESSAGE = "Exhibit mode is active and resets automatically after inactivity."
